---
"@nectar-js/create": minor
---

`npm create @nectar-js` picks the language and package manager from menus, asks for the bot's token, application ID, and test server ID and writes them to `.env`, and can install dependencies and create a git repository. New projects read the test server ID from `DEV_GUILD_ID`.
