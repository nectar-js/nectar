---
"@nectar-js/i18n": minor
---

New package: translations for Nectar bots. One JSON catalog per language, `t()` reading the locale off the interaction, `localizations()` feeding the same catalogs to Discord for command names and descriptions, generated types for every message key, and build-time reporting for languages that have fallen behind.
